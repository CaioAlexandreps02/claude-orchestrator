---
name: router-stats
description: Mostra estatisticas de uso do claude-orchestrator (economia estimada, mensagens por modelo)
---

# /router-stats

Atalho direto pra skill `router-stats` -- mesmo comportamento, sem precisar pedir em
linguagem natural. Ler `~/.claude/orchestrator-stats.json` e seguir exatamente as
instrucoes da skill `router-stats` (formato, ressalva sobre economia ser teto teorico,
aviso quando a amostra for pequena).
